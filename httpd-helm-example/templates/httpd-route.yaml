kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: httpd-route-helm
spec:
  host: {{ .Values.hostname }}
  to:
    kind: Service
    name: httpd-service-helm
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None